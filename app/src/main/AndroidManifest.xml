<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.livelearn">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!--SD卡讀寫-->
<!--    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>-->
<!--    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.basic_style">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value=" AIzaSyAJR6mil1VD8gktfb7JzP-dLbaBEMy5FsM" />

        <activity
            android:name=".MainActivity"
            android:label="樂齡享學">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps">
        </activity>
        <activity
            android:name=".College"
            android:label="社區大學">
        </activity>
        <activity
            android:name=".Map_item"
            android:label="詳細資訊" />
        <activity
            android:name=".Senior"
            android:label="樂齡中心"/>
        <activity
            android:name=".College_details"
            android:label="社區大學詳細資訊">
        </activity>
        <activity
            android:name=".SetUp"
            android:label="設定" >
        </activity>
        <activity
            android:name=".Notice"
            android:label="通知">
        </activity>
        <activity
            android:name=".Howtouse"
            android:label="使用教學">
        </activity>
        <activity
            android:name=".Color"
            android:label="顏色">
        </activity>
        <activity
            android:name=".Suggest"
            android:label="建議">
        </activity>
        <activity
            android:name=".LikeCourse"
            android:label="收藏課程">
        </activity>
        <activity
            android:name=".Schedule"
            android:label="課表">
        </activity>
        <activity
            android:name=".LearningCenter"
            android:label="樂齡中心">
        </activity>
    </application>

</manifest>